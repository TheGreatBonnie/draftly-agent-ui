/**
 * Application-shell boundary. The implementation remains compatible with the
 * existing shell while layout consumers migrate to focused layout modules.
 */
export { default } from "../dashboard/shell";
